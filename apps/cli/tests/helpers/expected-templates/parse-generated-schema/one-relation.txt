export const usersTable = p.pgTable('users', {
  id: p.text('id').primaryKey(),
});

export const postsTable = p.pgTable('posts', {
  id: p.text('id').primaryKey(),
  authorId: p.text('author_id'),
});