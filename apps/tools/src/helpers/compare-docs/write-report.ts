import { writeFile } from 'node:fs/promises';
import path from 'node:path';

import { type Report } from '#helpers/compare-docs/types.ts';

export const writeReport = async (output: string, report: Report) => {
  await writeFile(path.join(output, 'report.json'), JSON.stringify(report, null, 2));

  const payload = JSON.stringify(report).replaceAll('<', '\\u003c').replaceAll('\u2028', '\\u2028').replaceAll('\u2029', '\\u2029');

  await writeFile(
    path.join(output, 'index.html'),
    `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Docs comparison</title><style>
:root{color-scheme:light;--paper:#f5f3ed;--ink:#222722;--muted:#60665f;--line:#d5d8ce;--accent:#245b37}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:15px/1.5 ui-sans-serif,sans-serif}button,input,select{font:inherit}button,select,input{border:1px solid var(--line);border-radius:4px;background:#fff;color:var(--ink);padding:8px 12px}button{cursor:pointer}button:hover,button[aria-pressed=true]{background:var(--ink);color:#fff}a{color:var(--accent)}:focus-visible{outline:3px solid #db8719;outline-offset:3px}header{padding:28px 32px 20px;border-bottom:1px solid var(--ink)}.eyebrow{font:12px ui-monospace,monospace;letter-spacing:.15em;text-transform:uppercase;color:var(--accent)}h1{font:clamp(28px,4vw,44px)/1.2 Georgia,serif;margin:10px 0}h2{font:25px Georgia,serif;margin:0 0 8px}h3{margin:0;font-size:17px}.meta{color:var(--muted);overflow-wrap:anywhere}.stats{display:flex;gap:26px;flex-wrap:wrap;margin-top:20px}.stat strong{font:30px Georgia,serif;display:block}.stat span{font-size:12px;text-transform:uppercase;letter-spacing:.06em}.toolbar{display:flex;flex-wrap:wrap;gap:8px;padding:16px 32px;position:sticky;top:0;background:var(--paper);z-index:5;border-bottom:1px solid var(--line)}.toolbar input{flex:1;min-width:160px}.layout{display:grid;grid-template-columns:320px minmax(0,1fr);min-height:70vh}aside{border-right:1px solid var(--line);max-height:calc(100vh - 78px);overflow:auto;position:sticky;top:78px;align-self:start}.entry{display:block;width:100%;text-align:left;border:0;border-bottom:1px solid var(--line);border-radius:0;background:none;padding:14px 18px}.entry small{display:block;overflow-wrap:anywhere;opacity:.8}.entry strong{display:block;overflow-wrap:anywhere}.entry[aria-current=true]{background:#e0e9db;color:var(--ink);box-shadow:inset 4px 0 var(--accent)}.badge{font:11px ui-monospace,monospace;text-transform:uppercase;color:var(--accent)}.changed .badge,.new{color:#9b4b08}.removed .badge,.failed{color:#aa2b29}main{padding:24px;min-width:0}.controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin:16px 0}.panes{display:grid;grid-template-columns:1fr 1fr;gap:12px;align-items:start}.pane{min-width:0;margin:0}.pane figcaption{font:12px ui-monospace,monospace;margin-bottom:8px;overflow-wrap:anywhere}.pane img{display:block;width:100%;height:auto;border:1px solid var(--line);background:repeating-conic-gradient(#eee 0% 25%,#fff 0% 50%) 50%/16px 16px}.empty{padding:28px;background:#ecebe5;color:var(--muted)}.single{max-width:1100px}.overlay{position:relative;width:100%}.overlay img{width:100%;display:block}.overlay .front{position:absolute;inset:0;object-fit:contain;object-position:top left;clip-path:inset(0 50% 0 0)}.slider{width:240px}.notice{padding:14px 18px;border-left:3px solid #9b4b08;background:#f0e5d3;margin:14px 0;white-space:pre-wrap;overflow-wrap:anywhere}details{border-top:1px solid var(--line);padding:16px 32px}summary{cursor:pointer;font-weight:600}.diagnostic{padding:12px 0;border-bottom:1px solid var(--line);overflow-wrap:anywhere;white-space:pre-wrap}.diagnostic small{display:block;color:var(--muted)}pre{white-space:pre-wrap;overflow-wrap:anywhere;font-size:12px}.muted{color:var(--muted)}.toggle{display:flex;align-items:center;gap:6px;white-space:nowrap}.toggle input{flex:none;min-width:0}.count{float:right;font:11px ui-monospace,monospace;background:var(--ink);color:#fff;border-radius:10px;padding:1px 7px}.members{border-top:1px solid var(--line);margin-top:20px;padding:12px 0}.members button{display:block;width:100%;text-align:left;border:0;border-radius:0;background:none;padding:7px 0;border-bottom:1px solid var(--line)}.members button[aria-current=true]{font-weight:700}[hidden]{display:none!important}@media(max-width:850px){.layout{grid-template-columns:1fr}aside{position:static;max-height:260px;border-bottom:1px solid var(--ink)}.panes{grid-template-columns:1fr}header,.toolbar,details{padding-left:16px;padding-right:16px}main{padding:16px}}
</style></head><body><header><div class="eyebrow">Typebase / visual review</div><h1 id="title">Docs comparison</h1><div class="meta" id="meta"></div><div class="stats" id="stats"></div><div id="fatal"></div></header>
<details id="checks"><summary>Build and lint checks</summary><div></div></details>
<details><summary id="diagnostics-summary">Runtime and accessibility findings</summary><p class="muted">New, existing and resolved findings are compared separately. Automated accessibility checks cover default page states; manual keyboard and screen-reader checks are still needed.</p><div id="diagnostics"></div></details>
<details><summary>Coverage and limitations</summary><p>Routes are the union of both sitemaps, or the explicitly requested paths. Controls are matched by role, label, destination and occurrence. Renamed controls appear as added/removed. Hover captures include every discovered, visible, enabled button/link once per page and profile, including controls revealed inside disclosures. Disabled controls remain visible in page screenshots. Menus are discovered through ARIA, summary elements, and search-button labels. Unmarked custom menus and combinations of independent open panels need explicit manual checks.</p><p>Mobile touch has no hover. The mobile-hover profile uses a mouse at mobile width. Motion is disabled, time and locale are fixed, and analytics requests are suppressed. These comparisons do not test animation quality, physical devices, or production latency. Pixel differences indicate changes to review, not necessarily bugs.</p><div id="coverage-summary"></div><p><a href="report.json">Download complete report JSON</a> · <a href="base.json">Base capture inventory</a> · <a href="candidate.json">Candidate capture inventory</a></p><pre id="coverage"></pre></details>
<div class="toolbar"><input id="query" aria-label="Filter screenshots" placeholder="Find a page, control, or state…"><select id="status" aria-label="Status"><option value="review">Changes only</option><option value="all">All screenshots</option><option>changed</option><option>added</option><option>removed</option><option>identical</option></select><select id="profile" aria-label="Viewport and theme"><option value="all">All viewports / themes</option></select><select id="kind" aria-label="Screenshot kind"><option value="all">All states</option></select><label class="toggle"><input type="checkbox" id="group" checked> Group repeats</label><span class="muted" id="count"></span></div>
<div class="layout"><aside aria-label="Screenshots" id="list"></aside><main id="viewer"><div class="empty">Choose a screenshot to review.</div></main></div>
<script id="data" type="application/json">${payload}</script><script>
const report=JSON.parse(document.getElementById('data').textContent);const shots=report.comparisons||[];const $=id=>document.getElementById(id);const el=(tag,text,cls)=>{const node=document.createElement(tag);if(text!==undefined)node.textContent=text;if(cls)node.className=cls;return node};
$('title').textContent=(report.base?.label||'main')+' → '+(report.candidate?.label||'candidate');$('meta').textContent=(report.base?.commit?.slice(0,12)||report.base?.origin||'')+' / '+(report.candidate?.commit?.slice(0,12)||report.candidate?.origin||'')+' · '+report.started+' · '+(report.complete?'Completed':'Incomplete run');
for(const [label,value]of [['Screenshots',shots.length],['Changed',shots.filter(x=>x.status==='changed').length],['Added / removed',shots.filter(x=>['added','removed'].includes(x.status)).length],['New findings',(report.diagnostics||[]).filter(x=>x.status==='new').length]]){const box=el('div',undefined,'stat');box.append(el('strong',String(value)),el('span',label));$('stats').append(box)}
if(report.error)$('fatal').append(el('div',report.error,'notice'));else if(!report.complete)$('fatal').append(el('div','Capture coverage is incomplete. Review the runtime findings for failed or unverified interactions.','notice'));
for(const check of report.checks||[]){const row=el('p');row.append(el('strong',check.side+' / '+check.name+': '+check.status+' '));const link=el('a','View log');link.href=check.log;row.append(link);$('checks').lastElementChild.append(row)}
if(!(report.checks||[]).length)$('checks').lastElementChild.append(el('p','Existing-server mode: builds and lint were not run.'));
const findings=(report.diagnostics||[]).sort((a,b)=>({new:0,existing:1,resolved:2}[a.status]-{new:0,existing:1,resolved:2}[b.status]));$('diagnostics-summary').textContent='Runtime and accessibility findings ('+findings.length+')';for(const finding of findings){const row=el('div',undefined,'diagnostic '+finding.status);row.append(el('strong',finding.status.toUpperCase()+' · '+finding.type),el('small',finding.route+' · '+finding.profile+' · '+finding.state),el('div',finding.message));$('diagnostics').append(row)}
$('coverage-summary').textContent='Added routes: '+(report.routeChanges?.added||[]).join(', ')+' · Removed routes: '+(report.routeChanges?.removed||[]).join(', ')+' · Base routes: '+(report.routes?.base||[]).length+' · Candidate routes: '+(report.routes?.candidate||[]).length+' · Pixel color threshold: '+report.threshold;
$('coverage').textContent=JSON.stringify(report.coverage||{},null,2);
for(const [id,values]of [['profile',[...new Set(shots.map(x=>x.profile))]],['kind',[...new Set(shots.map(x=>x.kind))]]])for(const value of values)$(id).append(new Option(value,value));
let selected,mode='side';function image(src,alt){const img=el('img');img.src=src;img.alt=alt;img.loading='lazy';return img}function pane(src,label){const fig=el('figure',undefined,'pane');fig.append(el('figcaption',label));if(src){const link=el('a');link.href=src;link.target='_blank';link.rel='noopener';link.append(image(src,label));fig.append(link)}else fig.append(el('div','No matching capture','empty'));return fig}
function show(group){const shot=group.lead;selected=group;$('viewer').replaceChildren();for(const button of $('list').children)button.setAttribute('aria-current',String(button.dataset.key===group.id));const caption=el('div');caption.append(el('div',shot.status.toUpperCase()+' / '+shot.kind,'eyebrow'),el('h2',shot.route),el('h3',shot.label),el('div',shot.profile+' · '+shot.state,'meta'));if(group.members.length>1)caption.append(el('p','The same change appears on '+group.members.length+' captures across '+new Set(group.members.map(x=>x.route)).size+' routes. They are listed below; reviewing this one covers them all.','notice'));if(shot.dimensions)caption.append(el('p',shot.percent.toFixed(3)+'% pixels changed · '+shot.dimensions.before.join(' × ')+' → '+shot.dimensions.after.join(' × '),'meta'));$('viewer').append(caption);const controls=el('div',undefined,'controls');for(const [value,label]of [['side','Side by side'],['overlay','Overlay slider'],['diff','Pixel difference']]){const button=el('button',label);button.setAttribute('aria-pressed',String(mode===value));button.onclick=()=>{mode=value;show(group)};controls.append(button)}$('viewer').append(controls);if(mode==='side'){const panes=el('div',undefined,'panes');panes.append(pane(shot.before,report.base.label),pane(shot.after,report.candidate.label));$('viewer').append(panes)}else if(mode==='diff'){$('viewer').append(shot.diff?pane(shot.diff,'Changed pixels highlighted'):el('div','No pixel diff for identical or unmatched captures.','empty'))}else if(shot.before&&shot.after){if(shot.dimensions.before.join()!==shot.dimensions.after.join()){$('viewer').append(el('div','Image sizes differ. Use side-by-side or pixel difference for accurate alignment.','notice'));return}const overlay=el('div',undefined,'overlay single');overlay.append(image(shot.before,'Base'));const front=image(shot.after,'Candidate overlay');front.className='front';overlay.append(front);const slider=el('input',undefined,'slider');slider.type='range';slider.min=0;slider.max=100;slider.value=50;slider.setAttribute('aria-label','Candidate overlay reveal');slider.oninput=()=>front.style.clipPath='inset(0 '+(100-slider.value)+'% 0 0)';controls.append(slider);$('viewer').append(overlay)}else $('viewer').append(el('div','Overlay requires both captures.','empty'));
if(group.members.length>1){const list=el('div',undefined,'members');list.append(el('div','All '+group.members.length+' captures with this change','eyebrow'));for(const member of group.members){const button=el('button',member.route+'  ·  '+member.label+'  ·  '+member.state);button.setAttribute('aria-current',String(member.key===shot.key));button.onclick=()=>{group.lead=member;show(group)};list.append(button)}$('viewer').append(list)}}
function render(){const query=$('query').value.toLowerCase(),status=$('status').value,profile=$('profile').value,kind=$('kind').value;const filtered=shots.filter(x=>(status==='all'||(status==='review'?x.status!=='identical':x.status===status))&&(profile==='all'||x.profile===profile)&&(kind==='all'||x.kind===kind)&&[x.route,x.label,x.state].join(' ').toLowerCase().includes(query));const groups=[];const byId=new Map();for(const shot of filtered){const id=$('group').checked?(shot.group||shot.key):shot.key;let group=byId.get(id);if(!group){group={id,lead:shot,members:[]};byId.set(id,group);groups.push(group)}group.members.push(shot)}
$('count').textContent=$('group').checked?groups.length+' distinct change'+(groups.length===1?'':'s')+' in '+filtered.length+' captures':filtered.length+' captures';$('list').replaceChildren();for(const group of groups){const shot=group.lead;const button=el('button',undefined,'entry '+shot.status);button.dataset.key=group.id;const badge=el('span',shot.status,'badge');button.append(badge);if(group.members.length>1)button.append(el('span','×'+group.members.length,'count'));button.append(el('strong',shot.route),el('small',shot.label),el('small',shot.profile+' · '+shot.kind));button.onclick=()=>show(group);$('list').append(button)}if(groups.length)show(groups.find(x=>x.id===selected?.id)||groups[0]);else $('viewer').replaceChildren(el('div','No captures match this filter. Choose “All screenshots” to inspect unchanged pages.','empty'))}
for(const id of ['query','status','profile','kind','group'])$(id).addEventListener('input',render);render();
</script></body></html>`
  );
};
